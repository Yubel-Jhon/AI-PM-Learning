# 嘴替悬浮窗（本地脚本）

在钉钉输入框写好草稿 → 按 **Alt+Z** → 脚本抓选中文字 → 双引擎改写成高情商（云上千问 qwen3.8-flash 默认 / 本地 Ollama 隐私档）→ 粘回输入框 → 自动回车发出。

## 简介

**嘴替**是一个钉钉草稿改写悬浮窗：真心话先打进输入框，按 **Alt+Z**，脚本自动抓取草稿，
AI 改写成对方接得住、截图也不怕的说法再发出去。改写带两层质检——格式不过关自动重写，
**原意被改直接拦下**，保证话说得体面，但立场一个字不丢。

## 使用人群

- 天天在钉钉里沟通的职场人：怕话说重得罪人，怕说轻了自己吃亏
- 经常要拒绝不合理需求、催进度、回甩锅，又不想留下截图把柄的人
- 想让 AI 说话「像自己」而不是像客服的人（自我卡模仿你的口吻）
- 数据敏感、想演示「AI 全程本地跑」的隐私党（切 ollama 档）

## 使用场景

| 场景 | 你想打的 | 嘴替发出去的 |
|---|---|---|
| 被甩锅 | 这锅我不背，是你们需求没写清楚 | 这里和需求确认单对不上，我把记录拉出来对一下，明确了马上排。 |
| 被催活 | 催什么催，没看到我忙成什么样吗 | 手头压着两个急活，你的排明天上午；急的话说一声，我调下顺序。 |
| 要加急 | 凭什么让我白干，工时也不给算 | 这不在原排期内。要赶就得走加急流程把工时定下来，不然按原计划走。 |
| 群聊收场 | 行行行，都听你们的 | 那就这么定，决定权和锅一起归位，后面出状况咱一起复盘。 |

检测到火气自动推「强硬」档：边界先钉死，语气不失礼。

## 功能框架

```
嘴替悬浮窗 Alt+Z
│
├─ ① 触发与抓取
│   ├─ Alt+Z 全局热键（可改 config）
│   ├─ 全选→复制：抓输入框草稿
│   └─ 焦点捕获/归还：发送不粘错窗口
│
├─ ② 改写引擎（双引擎，失败自动回退）
│   ├─ api 档：云上 qwen3.8-flash（默认，2.7~3.7 秒）
│   ├─ ollama 档：本地 qwen2.5:7b（隐私，不出机）
│   ├─ 三档语气：常规 / 强硬（火气自动推档）/ 带损
│   ├─ 对象分寸：上级/平级/下属/客户/群聊 各有一套
│   ├─ 招式识别：甩锅/催/压/试探 → 先判招再落笔
│   └─ 自我卡：从你的历史消息学口吻
│
├─ ③ 质检闸门（两层，不过就重写）
│   ├─ 格式 lint：客服腔/脏字/说明泄漏/emoji 超量
│   ├─ 原意保持度：立场不翻转/数字不新增/时间不漂移
│   └─ 兜底链：带因重写 → 仍不过 → 本地规则版（保原意）
│
├─ ④ 预览窗（可选，关自动发出即弹）
│   ├─ 说明行：对象 / 档位 / 对方疑似招式
│   ├─ 流式改写：边出字边直播质检过程
│   └─ 可编辑：Enter 发框内文字 / Esc 放弃 / Alt+R 换一版
│
├─ ⑤ 记忆库（本地 SQLite，分层）
│   ├─ 关系卡：关键关系永久保留
│   ├─ 性格概括：1~3 月缓存，不重复分析
│   ├─ 近 1 月原文：现读
│   └─ 演示库：虚构人物，真库不上台
│
└─ ⑥ 配置与自检
    ├─ config.json：引擎/热键/模式/发送键
    ├─ local_settings.json：API key（不入库）
    └─ check_env.py：一键体检，每个 ✗ 带修法
```

---

## 先决条件

1. **Python 3.8+**（已检测到 3.12）。
2. 装依赖：双击 `安装依赖.bat`，或 `pip install -r requirements.txt`。
3. **引擎二选一**（`config.json` 的 `provider`，默认 `api`）：
   - **api 档（质量档，默认）**：云上大模型改写，效果最好。把 key 写进 `local_settings.json`
     （`{"api_key": "sk-…"}`，此文件已 gitignore，也可用环境变量 `DASHSCOPE_API_KEY`）。
     注意：草稿和上下文会发到云端 API——介意就切 ollama 档。
   - **ollama 档（隐私档）**：装并跑 [Ollama](https://ollama.com)：`ollama pull qwen2.5:7b`
     （或换模型后改 `config.json` 的 `model`），确认 `http://localhost:11434` 能访问。
     草稿和记忆库全程不出这台电脑——演示「全程本地」卖点时用这档。
   - api 档请求失败会自动回退本地模型，两者都不行才走规则兜底。

## 启动

双击 `启动嘴替.bat`。默认**收起成右下角一枚小图标**，不挡内容；点图标展开面板，点右上"收起 ⌄"或再点图标收回。想开机就展开，把 `config.json` 里 `start_expanded` 设为 `true`。

## 用法

1. 在钉钉对话框把话打完（哪怕是想骂人的原话）。
2. 鼠标点在输入框里，按 **Alt+Z**。
3. 脚本自动：全选→复制草稿→送 Ollama 改写→粘回→（默认）回车发出。

三个语气模式（悬浮窗上点，选中后下方会显示大白话说明；也可改 `config.json` 的 `mode`）：

- 高情商 `polish`：把你想说的话改得体面、有分寸，截图也不怕。
- 强硬 `firm`：立场更硬、先把边界钉死，但不失礼。
- 损一点 `drama`：更有梗更损，却还是发得出去的体面话。

> 想"替你回复对方发来的消息"，用聊天里的 `zuiti` 技能（说"嘴替 + 贴那条消息"），
> 悬浮窗只做"改写你正在输入框里打的话"这一件事。

## 隐私说明

`dumps/`（钉钉聊天记录抓取）与 `context.db`（记忆库）**只存在本地，不入本仓库**——都是真实对话数据。克隆本仓库后按上文流程用 `build_context_db.py` 自建。

---

## 上下文记忆库（可选，让改写"记得你俩聊过啥"）

悬浮窗自己调不动 dws（走 QwenWork 宿主认证），所以记忆库由 QwenWork 侧维护、脚本只读本地库：

- `context.db`（SQLite）：`conversations` / `messages`(近 1 月原文·强记忆) / `summaries`(更早摘要) / `relationship_cards`(永久关系卡)。
- 面板顶部「对象」下拉：选某个常用对话 → 改写时自动带上"关系卡 + 近期原文"作上下文；选「通用」→ 不带（退回无上下文）。
- 关系卡**不随时间衰减**，是"关键关系永久保留"的载体。
- 刷新方式：由 QwenWork 用 `dws chat +chat-messages --output …` 拉取，再跑 `python build_context_db.py` 重建库（可挂定时任务）。
- 增删常用对话：改 `build_context_db.py` 里的 `CONVS` 清单。

> 当前 v1：强记忆=最近 1 个月原文 + 关系卡；更早的"摘要"层已建好表结构，尚未自动生成（需要时再接模型压缩）。

## 关于"发送前拦截"的实话

钉钉是封闭客户端，**没有**发送前 hook。本脚本用系统级模拟逼近该效果（抓选区→改写→粘回→回车），
不是真钻进钉钉内部。因此：

- 需要你的光标焦点在钉钉输入框；脚本运行瞬间别切窗口。
- 剪贴板会被临时占用，结束会还原，但期间别手动复制。
- **默认自动回车发出**：万一改写不满意也已经发出去了。想稳一点，点悬浮条上"自动发出:开"切成"关(预览)"，
  会弹预览窗：改写**边出字边显示**（流式），顶部说明行交代对象/语气档/对方疑似招式，质检过程当场直播；
  出完字**框里可直接改**，Enter 发的是改过的版本 / Esc 放弃 / Alt+R 拿原始草稿换一版。
- 若你的钉钉是"Ctrl+Enter 发送"，把 `config.json` 的 `send_key` 改成 `ctrl+enter`。
- `grace_seconds` 是自动发出前的反悔缓冲秒数，期间按 Esc 可取消。

## 配置项（config.json）

| 键 | 含义 |
|---|---|
| provider | 引擎：`api`（云上大模型，默认）/ `ollama`（本地隐私档） |
| api_base | OpenAI 兼容端点，默认 DashScope；GLM 等兼容端点换这里即可 |
| api_model | api 档模型名，默认 `qwen3.8-flash`（qwen3 系已自动关思考；嫌慢/不稳换 `qwen-plus`）|
| ollama_host | Ollama 地址（ollama 档用） |
| model | 本地模型名，需与 `ollama list` 一致（ollama 档用） |
| hotkey | 全局热键，`keyboard` 库格式，如 `alt+z` / `ctrl+alt+space` |
| mode | 默认模式 polish/firm/drama/reply |
| auto_enter | true 自动发出；false 弹预览 |
| send_key | 发送键 enter 或 ctrl+enter |
| grace_seconds | 自动发出前的反悔缓冲秒 |
| keep_alive | 模型常驻内存时长（默认 `30m`），启动时会后台预热，首次改写不用等冷启动 |
| num_ctx | 上下文窗口（默认 `4096`）。Ollama 默认 2048，带记忆库改写会被截断 |
| temperature | 默认 `0.5`，改写求稳；“换一版”会临时 +0.25 |
| mimic_me | 自我卡开关（默认开），右键菜单也能切。从库里你的历史消息提炼口吻，让成稿像你本人 |
| my_name | 你在钉钉里的名字（已写死“大宇”）。自动检测不可靠（发言最多的往往是别人），建议写死 |

## 改写质量（v2）

- 提示词带**每档语气 3 条示例**（7B 模型对示例比对指令敏感），先判断对方那句话在干什么（甩锅/催/压/正常沟通）再落笔。
- 成稿有**质检**，两层：
  - **格式 lint**：客服腔（收到~/感谢您的理解与支持/请您放心…）、残留脏字、说明性文字泄漏、emoji 超量；
  - **原意保持度**：拒绝被改成答应、答应被改成拒绝、成稿出现原话没有的数字、时间口径漂移（周三→周五）——事实被改走本地规则兜底，绝不发出去。
  - 任一层不过 → 自动带违规原因重试一次。
- **自我卡（模仿你的语气）**：从 `context.db` 里你发过的消息提炼说话风格（句长/称呼/语气词/emoji 习惯）+ 5 条原话样本注入提示词，成稿模仿你本人的口吻。改写硬规则优先于风格。库缺你的消息或消息少于 8 条时自动不注入。
- 预览模式的“换个说法”**拿原始草稿重改**（之前是在上一版上继续改，意思会越漂越远），且预览窗**不再 30 秒不选就自动发出**——不选就一直等。
- **预览窗三件套（v3）**：①说明行——对象/语气档（自动推档会交代“检测到火气”）/对方疑似招式识别（本地规则推断，标“疑似”）；②流式改写——边出字边进框，质检不过当场清框重写，全程看得见；③可编辑成稿——Enter 发的是框里当前文字（先改再发），发送前自动把焦点还给 Alt+Z 时的窗口，不会粘错地方。

## 演示模式（真库不上台）

现场展示、录屏或把截图发给别人时，用脱敏演示库，**别用真库**（`context.db` 是真实钉钉记录，下拉里会出现真名真群名）。

1. `python build_demo_db.py` —— 生成 `demo_context.db`：三个虚构对象（张总/王姐/项目推进群），每个带一组脚本化对话记忆 + 人设卡；"大宇"的历史消息同时喂自我卡。重跑即重建，幂等。
2. 双击 **`启动嘴替-演示.bat`** 启动 —— 它只做一件事：设环境变量 `ZUITI_DB=demo_context.db` 再拉起脚本。悬浮窗右侧菜单「重载上下文库」后，对象下拉里只会出现张总/王姐/项目推进群。
3. 配套 **`演示剧本.md`**：开演前检查清单、五个排练场景（含每条该说的讲解词）、翻车预案。

原理：`context.py` 里 `DB = os.environ.get("ZUITI_DB") or .../context.db`，演示库与真库物理分文件，不改一行代码切换；真库文件在本机原样保留。

## 路径与自检（出了问题先跑这个）

| 东西 | 定义在哪 | 默认 |
|---|---|---|
| API key（api 档引擎） | `local_settings.json`（脚本同目录，gitignore 排除）或环境变量 `DASHSCOPE_API_KEY` | 无 |
| 记忆库 context.db | `context.py` 的 `DB` | 脚本同目录；环境变量 `ZUITI_DB` 可覆盖 |
| dumps 钉钉导出 | `build_context_db.py` 的 `DUMPS` | 脚本同目录 dumps/ |
| 演示库 | `build_demo_db.py` | 脚本同目录 demo_context.db |
| 人物卡输出 | `persona_export.py` | 库同目录 persona_cards/ |
| MBTI 钦点记录 | `persona_export.py --set-mbti` | persona_cards/_mbti_override.json |

读库失败**不再静默**：控制台会打 `[context] xxx 失败: 原因`，悬浮窗就绪状态栏会写
"引擎 xxx · 记忆库 X会话/X条"（没建库会明说"只走通用模式"，没配 key 会明说"回退本地"）。

**双击 `检查环境.bat`**（或 `python check_env.py`）一次看全：生效库是哪个、每个会话多少条、
自我卡认没认对人、dumps 缺不缺导出、config 生效值、Ollama 在不在线、人物卡落没落。
每一项 ✗ 后面都跟着"修：xxx"。

## 停止

右键悬浮条 → 退出；或关闭命令行窗口。热键失效多半是 `keyboard` 需要权限，
用管理员身份重跑 `启动嘴替.bat`。
